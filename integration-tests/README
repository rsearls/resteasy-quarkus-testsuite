
The contents in directory src/main are required so quarkus-maven-plugin can
create a jar from mvn "install" cmd.  This is needed as of plugin
version 1.3.0.Final and could change in the future.



Execution env.
    jdk 11.0.2
    mvn 3.6.3
    quarkus 999-SNAPSHOT
    
  The pom.xml file contains the minimum archives required to compile and run the tests.
  See lines 268-278 for the set of tests being run.
  
  
  Compile project:
    mvn clean test -DskipTests=true
  
  Run tests
    mvn test
    or
    mvn test -Dmaven.surefire.debug
  
  Debugging mvn compile process
  export MAVEN_OPTS="-Xmx2048m -XX:MaxPermSize=1536m -agentlib:jdwp=transport=dt_socket,address=8787,server=y,suspend=y"
  
  